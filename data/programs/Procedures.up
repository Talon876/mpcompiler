program Main;
  var a, b : integer;

  procedure Fred(x : integer);
    var t : integer;
    begin {Fred}
      t := x + 1;
      writeln(t);
      end; {Fred;}
      
  procedure Mary(c : integer; var d: integer);
    var i : integer;
    begin {Mary}
      read(i);
      Fred(i);
      c := c + a;
      d := d + 1;
      writeln(c+d);
      end; {Mary}

  begin {Main}
    read(a,b);
    Mary(a,b);
  end. {Main}
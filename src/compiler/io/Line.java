package compiler.io;

public class Line {

    /**
     * 
     * @param fullLine
     *            the entire line from the file (without the newline character at the end)
     */
    public Line(String fullLine) {

    }
}
